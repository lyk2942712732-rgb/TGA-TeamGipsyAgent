{
  "mcpSecurityHub": {
    "source": "https://github.com/FuzzingLabs/mcp-security-hub",
    "localRoot": "D:/path/to/mcp-security-hub",
    "mode": "full-catalog-policy-gated"
  },
  "policy": {
    "default": "deny unless target is in scope and intensity allows the tool",
    "destructiveTools": "registered but disabled until an explicit contract flag exists"
  }
}

