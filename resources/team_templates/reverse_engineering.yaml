{
  "mode": "reverse_engineering",
  "supervisor_definition_id": "reverse-supervisor",
  "required_solver_definition_ids": ["binary-triage-solver"],
  "available_solver_definition_ids": ["binary-triage-solver", "static-analysis-solver", "dynamic-analysis-solver", "logic-config-recovery-solver"],
  "reviewer_definition_id": "evidence-reviewer",
  "reporter_definition_id": "security-reporter",
  "spawn_rules": [
    {"trigger": "task_start", "definition_id": "reverse-supervisor", "max_instances": 1},
    {"trigger": "intent_ready", "definition_id": "binary-triage-solver", "max_instances": 1},
    {"trigger": "review_required", "definition_id": "evidence-reviewer", "max_instances": 1},
    {"trigger": "report_required", "definition_id": "security-reporter", "max_instances": 1}
  ],
  "max_active_workers": 2,
  "max_total_solvers": 9,
  "completion_policy": {"supervisor_decides": true, "require_reviewer": true, "require_reporter": true, "require_all_required_intents_terminal": true}
}
