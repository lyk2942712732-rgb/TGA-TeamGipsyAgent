{
  "mode": "penetration_test",
  "supervisor_definition_id": "pentest-supervisor",
  "required_solver_definition_ids": ["surface-mapper"],
  "available_solver_definition_ids": ["surface-mapper", "web-api-analyst", "vulnerability-validator"],
  "reviewer_definition_id": "evidence-reviewer",
  "reporter_definition_id": "security-reporter",
  "spawn_rules": [
    {"trigger": "task_start", "definition_id": "pentest-supervisor", "max_instances": 1},
    {"trigger": "intent_ready", "definition_id": "surface-mapper", "max_instances": 1},
    {"trigger": "review_required", "definition_id": "evidence-reviewer", "max_instances": 1},
    {"trigger": "report_required", "definition_id": "security-reporter", "max_instances": 1}
  ],
  "max_active_workers": 2,
  "max_total_solvers": 8,
  "completion_policy": {"supervisor_decides": true, "require_reviewer": true, "require_reporter": true, "require_all_required_intents_terminal": true}
}
