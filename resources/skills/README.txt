Skill resources live here as Markdown documents grouped by specialty.

The legacy SkillRegistry and the new infrastructure FileSkillCatalog both read
this directory. Task-level schema-v5 snapshots remain compatible, while new
selection services freeze task-common and solver-specialized snapshots.

