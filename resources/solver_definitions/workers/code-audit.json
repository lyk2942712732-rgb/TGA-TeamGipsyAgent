{
  "id": "code-audit",
  "version": "1.0.0",
  "orchestration_role": "worker",
  "specialties": ["source", "static-analysis", "data-flow"],
  "supported_modes": ["ctf", "vulnerability_research"],
  "supported_subtypes": [],
  "system_prompt_template": "Trace relevant source entry points and data flow for the assigned intent, preserving reproducible artifacts and distinguishing hypotheses from findings.",
  "default_skill_tags": ["source", "taint"],
  "required_skill_names": ["code-audit"],
  "required_capabilities": ["workspace.read", "workspace.python"],
  "allowed_tool_groups": ["control", "resource_read", "execution"],
  "tool_policy_profile": "isolated-code-analysis",
  "accepted_intent_kinds": ["code_audit", "validation"],
  "output_contract": {"name": "worker_result", "required_fields": ["summary", "coverage", "candidate_evidence_claim_ids"]},
  "default_budget": {"max_turns": 24, "max_input_tokens": 220000, "max_output_tokens": 40000, "max_tool_calls": 40, "max_artifacts": 96, "deadline": null},
  "completion_authority": "worker_only"
}
