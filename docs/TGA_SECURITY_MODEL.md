# Historical security model pointer

The retired single-Agent schema-v5 execution model has no current Runtime path. The
authoritative security contract is [architecture/SECURITY_MODEL.md](architecture/SECURITY_MODEL.md).

Schema v5 is rejected by the application, API, and Runtime and can only be read by the
explicit offline migration tool.
