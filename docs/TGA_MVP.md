# TGA MVP

Week 1 builds a local, evidence-first MVP for authorized targets and CTF labs.

Deliverables:

- task config loader
- sequential planner/scheduler
- subprocess worker
- tool policy and MCP healthcheck
- SQLite evidence store
- filesystem artifact store
- flag and finding gates
- Markdown report

