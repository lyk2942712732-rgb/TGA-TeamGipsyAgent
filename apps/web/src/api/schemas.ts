import { z } from "zod";

const status = z.enum(["created", "running", "paused", "blocked", "completed", "failed", "cancelled"]);
// Events are an append-only, versioned audit stream. Their payload gains new
// fields over time, so validate the stable envelope and preserve unknown data.
export const AgentEventSchema = z.object({ id: z.string().or(z.number()).transform(String), task_id: z.string().optional().default(""), solver_id: z.string().nullable().optional(), seq: z.number().int().positive(), type: z.string(), payload: z.record(z.string(), z.unknown()).default({}), created_at: z.string().optional().default("") });
export const RuntimeSnapshotSchema = z.object({ task: z.object({ id: z.string().default(""), name: z.string().default("未命名 Session"), mode: z.enum(["ctf", "web_audit", "code_audit", "binary_ctf"]).catch("ctf"), target: z.string().default(""), scope: z.array(z.string()).default([]) }), session: z.object({ status, turn_count: z.number().int().nonnegative(), max_turns: z.number().int().positive(), active_solver_id: z.string().nullable().optional(), stop_reason: z.string().nullable().optional() }), solvers: z.array(z.object({ id: z.string(), role: z.string().default("main"), status: z.string(), model_name: z.string().optional(), started_at: z.string().nullable().optional(), finished_at: z.string().nullable().optional() })).default([]), board: z.object({ hypotheses: z.array(z.object({ id: z.string(), statement: z.string(), attack_class: z.string(), entry_point: z.string(), rationale: z.string().default(""), next_test: z.string().default(""), status: z.enum(["pending", "testing", "verified", "rejected", "inconclusive", "superseded"]).catch("pending"), confidence: z.number().min(0).max(1).default(0), attempt_count: z.number().int().nonnegative().default(0), evidence_artifact_ids: z.array(z.string()).default([]), last_result: z.string().default(""), owner_solver_id: z.string().nullable().optional(), created_at: z.string().optional(), updated_at: z.string().optional() })).default([]), memory: z.array(z.object({ id: z.string(), kind: z.enum(["fact", "evidence", "failure_boundary", "hint", "constraint", "decision"]).catch("fact"), content: z.string(), artifact_ids: z.array(z.string()).default([]), source: z.string().default("runtime"), supersedes_id: z.string().nullable().optional(), created_at: z.string().optional(), updated_at: z.string().optional() })).default([]) }), actions: z.array(z.object({ id: z.string(), capability: z.string(), target: z.string().default(""), status: z.enum(["proposed", "approved", "running", "succeeded", "failed", "blocked", "cancelled"]).catch("proposed"), hypothesis_id: z.string().nullable().optional(), rationale: z.string().optional(), summary: z.string().optional(), artifact_ids: z.array(z.string()).default([]), error: z.object({ code: z.string().optional(), message: z.string().optional() }).nullable().optional(), created_at: z.string().optional(), updated_at: z.string().optional() })).default([]), flags: z.array(z.object({ value: z.string(), evidence_artifact_id: z.string(), created_at: z.string().optional() })).default([]), findings: z.array(z.object({ id: z.string(), title: z.string(), target: z.string(), severity: z.string(), status: z.enum(["candidate", "confirmed", "rejected"]), evidence_artifact_id: z.string().nullable().optional(), evidence_excerpt: z.string().nullable().optional(), remediation: z.string().nullable().optional() })).default([]), artifacts: z.array(z.object({ id: z.string(), task_id: z.string().optional(), kind: z.string(), path: z.string(), tool: z.string().nullable().optional(), target: z.string().nullable().optional(), created_at: z.string().optional(), excerpt: z.string().optional(), status: z.number().optional(), method: z.string().optional(), truncated: z.boolean().optional() })).default([]), events: z.array(AgentEventSchema).default([]), latest_seq: z.number().int().nonnegative() });
